APP_NAME=MyApp
APP_ENV=local        # local | production
APP_BASE_PATH=       # empty if served from domain root, e.g. /php-starter/public for subdirectory

DB_HOST=localhost
DB_NAME=
DB_USER=root
DB_PASS=
